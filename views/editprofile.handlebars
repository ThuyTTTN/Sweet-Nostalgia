<div class="dashboard-hero">
    <h1 class="mb-5">Edit Account Information</h1>
    <div class="row">
        <div class="edit-profile-form">
                <form class="row g-3 requires-validation" novalidate>


                             <div class="col-md-6">
                        <label for="edit-email" class="form-label">Email:</label>
                        <div class="input-group">
                            <input type="email" class="form-control" id="edit-email" value="{{users.email}}" required>
                            <span class="input-group-text" id="basic-addon2">@gmail.com</span>
                            <div class="valid-feedback text-info">
                                Email looks good!
                            </div>
                            <div class="invalid-feedback text-warning">
                                Email is required!
                            </div>
                        </div>
                    </div>

                     <div class="col-md-12">
                        <label for="edit-first-name" class="form-label">First name:</label>
                        <input type="address" class="form-control" id="edit-first-name" value="{{users.first_name}}" required>
                        <div class="valid-feedback text-info">
                            Address looks good!
                        </div>
                        <div class="invalid-feedback text-warning">
                            Addres is required!
                        </div>
                    </div>

                    <div class="col-md-12">
                        <label for="edit-last-name" class="form-label">Last Name:</label>
                        <input type="address" class="form-control" id="edit-last-name" value="{{users.last_name}}" required>
                        <div class="valid-feedback text-info">
                            Address looks good!
                        </div>
                        <div class="invalid-feedback text-warning">
                            Addres is required!
                        </div>
                    </div>



                    {{!-- <div class="col-md-12 pt-3">
                        <div class="input-group">
                            <span class="input-group-text" for="edit-first name edit-last-name">First and last
                                name</span>
                            <input type="name" class="form-control" id="edit-first-name" value="{{users.first_name}}" required>
                            <input type="name" class="form-control" id="edit-last-name" value="{{users.last_name}}" required>
                            <div class="invalid-feedback text-warning">
                                First and last name is required
                            </div>
                        </div>
                    </div> --}}

                    <div class="col-md-12">
                        <label for="edit-address" class="form-label">Address:</label>
                        <input type="address" class="form-control" id="edit-address" value="{{users.address}}" required>
                        <div class="valid-feedback text-info">
                            Address looks good!
                        </div>
                        <div class="invalid-feedback text-warning">
                            Addres is required!
                        </div>
                    </div>

                    <div class="col-md-6">
                        <label for="edit-city" class="form-label">City:</label>
                        <input type="city" class="form-control" id="edit-city" value="{{users.city}}" required>
                        <div class="valid-feedback text-info">
                            City looks good!
                        </div>
                        <div class="invalid-feedback text-warning">
                            City is required!
                        </div>
                    </div>

                    <div class="col-md-4">
                        <label for="edit-state" class="form-label">State:</label>
                        <input type="state" id="edit-state" class="form-control" value="{{users.state}}" required>
                           <div id="state-helper" class="form-text ">
                            Input your state's abbreviation (ex. CA)
                        </div>
                        <div class="valid-feedback text-info">
                            State looks good!
                        </div>
                        <div class="invalid-feedback text-warning">
                            State is required!
                        </div>
                    </div>

                    <div class="col-md-2">
                        <label for="edit-zipCode" class="form-label">ZipCode:</label>
                        <input type="zipcode" class="form-control" id="edit-zipCode" value="{{users.zipCode}}" required>
                        <div class="valid-feedback text-info">
                            ZipCode looks good!
                        </div>
                        <div class="invalid-feedback text-warning">
                            ZipCode is required!
                        </div>
                    </div>

                    <div class="col-12">
                        <button class="btn btn-info" type="submit">Save Changes</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    
    <script src="/js/edit-profile.js"></script>
    <script src="/js/editprofile-validate.js"></script>

            {{!-- <form class="row g-3">
                <div class="col-md-6 pt-3">
                    <label for="first_name" class="form-label">First Name:</label>
                    <input type="text" class="form-control" id="edit-first-name" placeholder="{{users.first_name}}">
                </div>

                <div class="col-md-6 pt-3">
                    <label for="last_name" class="form-label">Last Name:</label>
                    <input type="text" class="form-control" id="edit-last-name" value="{{users.last_name}}"></input>
                </div>

                <div class="col-md-6 pt-3">
                    <label for="email" class="form-label">Email:</label>
                    <input type="text" class="form-control" id="edit-email" placeholder="{{users.email}}">
                </div>

                <div class="col-md-6 pt-3">
                    <label for="address" class="form-label">Address:</label>
                    <input type="address" class="form-control" id="edit-address" placeholder="{{users.address}}">
                </div>

                <div class="col-md-6 pt-3">
                    <label for="city" class="form-label">City:</label>
                    <input type="city" class="form-control" id="edit-city" placeholder="{{users.city}}">
                </div>

                <div class="col-md-6 pt-3">
                    <label for="state" class="form-label">State:</label>
                    <input type="state" class="form-control" id="edit-state" placeholder="{{users.state}}">
                </div>

                <div class="col-md-6 pt-3">
                    <label for="zipCode" class="form-label">ZipCode:</label>
                    <input type="zipCode" class="form-control" id="edit-zipCode" placeholder="{{users.zipCode}}">
                </div>

                <div class="col-md-12">
                    <button type="submit" class="btn btn-primary">Save Changes</button>
                </div>

            </form>

        </div> --}}
